class Window {
    sf::RenderWindow;
    RType::EventHandler;
    ...
};

class EventHandler {
    CustomEvent;

    Observer;
    broadcast(Observer);
};

template<class RType::RenderTarget>
class Renderer {
    RType::RenderTarget;
};

class Observer {
    /* Observer Pattern */
};

class SFMLTYPES ... {
    vector, uint8, ...
};

namespace Network
{

struct ComponentUDP;
struct ComponentTCP;

struct ComponentInput;
struct ComponentOutput;

struct ComponentNetworkEventRecorder;

} // namespace Network
